<h1>Flights</h1>

<% @flights.each do |flight| %>
  <div id = "<%=flight.number%>">
  <%= "Flight Number: #{flight.number}, Airline: #{flight.airline.name}"%>
  <p>Passengers <p>
  <ul>
  <% flight.passengers.each do |pass|  %>
   <li> <%= pass.name %></li>
  <% end %>  
  </ul>
  </div>
<% end %>

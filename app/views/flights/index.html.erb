<body>
  <section id="flight_list">
    <ol>
      <p><% @flights.each do |flight| %>
        <div id="flight_details-<%= flight.id %>">
          <p>Flight Details: <%=  flight.name %> - #<%= flight.number %></p>
          <p>Passengers:</p>
          <% flight.passengers.each do |passenger| %>
            <li><%= passenger.name %></li>
          <% end %>
        </div>
      <% end %>
      </p>
    </ol>
  <section>